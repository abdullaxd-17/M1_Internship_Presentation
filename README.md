# LoRa Telemetry Presentation

Open `index.html` to run the presentation. All main assets are kept in the same folder for simple GitHub Pages deployment.

The video window first looks for a local file named `demo_video.mp4`. If that file is not present, the slide falls back to the embedded YouTube demonstration inside the same window.
